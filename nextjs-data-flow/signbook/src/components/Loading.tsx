import { ReactElement } from 'react';

const Loading = (): ReactElement => {
  return (
    <div className='min-h-screen w-screen flex justify-center items-center'>
      Loaidng signs from Hasura...
    </div>
  );
};

export default Loading;
